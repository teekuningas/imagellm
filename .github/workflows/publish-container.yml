name: Build and Publish

on:
  push:
    tags:
      - '*'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Extract tag name
      shell: bash
      run: echo "IMAGE_TAG=${GITHUB_REF#refs/tags/}" >> $GITHUB_ENV

    - name: Build
      run: |
        docker build -f Containerfile -t localhost/imagellm-frontend:$IMAGE_TAG .
        docker save localhost/imagellm-frontend:$IMAGE_TAG | gzip > imagellm-frontend-$IMAGE_TAG.tar.gz

    - name: Create Release
      id: create_release
      uses: actions/create-release@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        tag_name: ${{ github.ref }}
        release_name: Release ${{ github.ref }}
        draft: false
        prerelease: false

    - name: Upload Release
      uses: actions/upload-release-asset@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        upload_url: ${{ steps.create_release.outputs.upload_url }}
        asset_path: ./imagellm-frontend:${{ env.IMAGE_TAG }}.tar.gz
        asset_name: imagellm-frontend:${{ env.IMAGE_TAG }}.tar.gz
        asset_content_type: application/gzip
